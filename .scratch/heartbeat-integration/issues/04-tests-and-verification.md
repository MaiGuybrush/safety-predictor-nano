# Issue 04: 單元測試與整合驗證

**Role:** ready-for-agent

**What to build:**
撰寫單元測試與整合測試，驗證 `argus-eventlog` 與 `safety-predictor-nano` 的 Heartbeat 相關邏輯與 Camera ID 解析。

**Acceptance criteria:**
- [x] `argus-eventlog` 測試全部通過。
- [x] `safety-predictor-nano` 新增 `test_heartbeat_integration.py`。
- [x] 完整 pytest 測試套件無回歸錯誤。
